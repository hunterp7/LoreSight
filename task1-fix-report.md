Task 1 review findings addressed:

- Updated the launch-surface test to match the current `/desk` and `/demo` route shape without weakening the admin boundary.
- Added a static submission-validator scan over public app metadata and tool descriptors to block `/admin`, private actor fields, hidden canon, and raw filesystem data from leaking into the public contract.

Verification still needs to be run after this edit.
